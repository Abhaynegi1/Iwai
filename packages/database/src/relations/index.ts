/**
 * IWAI Database Relations
 *
 * Drizzle ORM relations are defined here.
 * These complement the schema and enable relational queries
 * using the Drizzle query API (db.query.*).
 *
 * Relations will be added here alongside their corresponding schema tables.
 *
 * Example (to be added when tables exist):
 *   import { relations } from "drizzle-orm";
 *   import { events, photos } from "../schema/index.js";
 *
 *   export const eventsRelations = relations(events, ({ many }) => ({
 *     photos: many(photos),
 *   }));
 */

// Relations barrel — add domain relations here as tables are created
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export {};

